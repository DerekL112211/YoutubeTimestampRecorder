package com.example;

/**
 * Enum to define timestamp types for hierarchical structure
 */
public enum TimestampType {
    MAIN,    // Main timestamp with no indentation
    SUB      // Sub-timestamp with indentation
}
